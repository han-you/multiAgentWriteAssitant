遇到的问题：
researcher_node使用duckduckgo_search搜索的时候涉及到网页请求，若网页请求不成功会直接导致程序奔溃无法生成行业现状报告，加入try except，在网络请求失效的情况下，模型调用已有的知识生成报告。
research搜集到的数据有噪声会干扰writer_node生成报告，需要筛选，比如将body小于指定长度的数据项删除,或者直接调用一次LLM把原始结果压缩成结构化摘要，代价是多调用一次api，但是输出质量明显提升
